{
  "name": "@cryptra/workers",
  "version": "0.1.0",
  "private": true,
  "description": "Background workers (BullMQ) for Cryptra",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cryptra/config": "workspace:*",
    "@cryptra/database": "workspace:*",
    "@cryptra/queue": "workspace:*",
    "@cryptra/notifications": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.19.1",
    "@types/node": "^22.10.0"
  }
}
